changelog:
  - type: FIX
    description: >
      `glooctl` commands such as `glooctl get vs` now work without cluster-scoped rbac permissions.
    issueLink: https://github.com/solo-io/gloo-edge/issues/2375
