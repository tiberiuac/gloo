changelog:
- type: FIX
  description: >
    `glooctl uninstall` now respects all the CLI options when Gloo was not installed via `glooctl install` or `helm install`.
  issueLink: https://github.com/solo-io/gloo-edge/issues/1938